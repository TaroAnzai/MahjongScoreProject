# --- Flask & Extensions ---
Flask>=3.0,<4.0
Flask-Cors>=5.0,<6.0
Flask-Login>=0.6,<1.0
Flask-Migrate>=4.0,<5.0
Flask-SQLAlchemy>=3.0,<4.0
# marshmallow 3 と互換性のある範囲
flask-smorest>=0.45,<0.48
flask-limiter>=3.0,<5.0

# --- Database ---
SQLAlchemy>=2.0,<3.0
PyMySQL>=1.0,<2.0

# --- Alembic (DBマイグレーション) ---
alembic>=1.14,<2.0

# --- Utilities ---
marshmallow>=3.21,<4.0
python-dotenv>=1.0,<2.0
cryptography>=45.0,<46.0   # MySQL 8.0 認証で必須
requests>=2.32,<3.0

# --- Gunicorn (本番WSGIサーバー) ---
gunicorn>=23.0,<24.0

# --- Testing（このファイルを使う Docker・本番環境にもインストールされる） ---
pytest>=8.0,<9.0
pytest-cov>=7.0,<8.0

# --- アプリケーションが直接使用するライブラリ ---
Werkzeug>=3.0,<4.0
Jinja2>=3.1,<4.0

# --- Celery with Redis ---
celery[redis]==5.4.0
