server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    location = /mahjong { return 301 /mahjong/; }
    location /mahjong/assets/ { try_files $uri =404; }
    location /mahjong/ { try_files $uri $uri/ /mahjong/index.html; }
    location / { return 404; }
}
